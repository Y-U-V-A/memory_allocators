#ifndef UTILS__H
#define UTILS__H

#include "defines.h"

void random_seed();

f64 random_ndc();

u32 random_int(u32 min, u32 max);

f32 random_float(f32 min, f32 max);

f64 random_double(f64 min, f64 max);

#endif